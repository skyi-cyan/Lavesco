rules_version = '2';
// 프로필 사진: users/{userId}/* — 해당 사용자만 읽기/쓰기, 인증된 사용자는 읽기 가능
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
